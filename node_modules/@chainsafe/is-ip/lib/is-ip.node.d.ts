import { isIPv4, isIPv6, isIP as ipVersion } from "node:net";
export { isIPv4, isIPv6, ipVersion };
/** Check if `input` is IPv4 or IPv6. */
export declare function isIP(input: string): boolean;
//# sourceMappingURL=is-ip.node.d.ts.map